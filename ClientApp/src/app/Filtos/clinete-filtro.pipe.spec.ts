import { ClineteFiltroPipe } from './clinete-filtro.pipe';

describe('ClineteFiltroPipe', () => {
  it('create an instance', () => {
    const pipe = new ClineteFiltroPipe();
    expect(pipe).toBeTruthy();
  });
});
